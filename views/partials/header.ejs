<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/orders">Niam Niam</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>


        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav my-2 my-lg-0 ml-auto">
             <% if (!currentUser) { %>
            <li class="nav-item">
              <a class="nav-link" href="/login">Prisijungti</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/register">Registruotis</a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href='/user/<%= currentUser.id %>'><%= currentUser.username %>
              <% if(currentUser.isAdmin == true) { %>
                (Admin)
                <% } %>
              </a>
            </li>
            <li>
              <a class="nav-link" href="/logout">Atsijungti</a>
            </li>
            <% } %>
          </ul>
        </div>
      </nav>